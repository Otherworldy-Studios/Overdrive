using UnityEngine;

namespace Overdrive.Enemies
{
/// <summary>
/// These are the tier catagories for the baseline of enemy scaling
/// Tier dif affects values like health and such, not behaviour
/// </summary>
    public enum EnemyTier
    {
    Minor = 0,
    Standard = 1,
    Elite = 2
    }
}
